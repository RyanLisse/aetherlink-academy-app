# assets: env names only. Set values as OpenShip runtime secrets on academy-assets-poc; never commit a filled copy.
# Contract and open names: infra/native-apps/PROVIDERS.md
DATABASE_URL=
# ASSETS_STORAGE_* keys are open until confirmed against upstream templates/assets.
