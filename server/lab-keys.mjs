// Server-only answer keys for graded lab stops: {[labId]: {[stopId]: key}}. Arcade stop ids are
// `stop-<n>`, counting the lesson's checkpoints from 1. Never import this from the client or put it
// in a day pack, which the portal publishes whole. No production pack declares labs yet.
export const labGradingKeys={};
