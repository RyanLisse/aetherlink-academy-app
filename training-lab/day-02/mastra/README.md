# Optional Mastra path

Port the day-2 weather agent to Mastra if that framework is available in the
participant's own environment. Keep the same local HTTP input and decision
contract (`bring umbrella` at ≥60%). No dedicated Academy lesson — README only (AET-37).
