/** Verbatim source block from aetherlab monolith (id=src-eve-tool). */
export default "import { defineTool } from \"eve/tools\";\nimport { never } from \"eve/tools/approval\";\nimport { z } from \"zod\";\n\nexport default defineTool({\n  approval: never(),\n  description: \"Return simulated weather for a city. This is not live weather.\",\n  inputSchema: z.object({ city: z.string().trim().min(1) }),\n  async execute({ city }) {\n    return {\n      city,\n      temperatureF: 72,\n      condition: \"Sunny\",\n      summary: `Sunny in ${city} with a light breeze.`,\n    };\n  },\n});\n" as string;
